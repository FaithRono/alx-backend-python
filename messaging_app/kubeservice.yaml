apiVersion: v1
kind: Service
metadata:
  name: django-service-blue-green
spec:
  selector:
    app: django-messaging-app
    version: blue  # Initially pointing to blue
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: django-service-blue
spec:
  selector:
    app: django-messaging-app
    version: blue
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: django-service-green
spec:
  selector:
    app: django-messaging-app
    version: green
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: ClusterIP